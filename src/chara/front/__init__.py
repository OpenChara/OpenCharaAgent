"""Frontends: the CLI entry, the Textual TUI, the plain terminal, the roster
launcher and the setup wizard. The ONLY packages allowed to import textual —
and (until the CharaHandle facade lands) the only ones touching core directly."""
