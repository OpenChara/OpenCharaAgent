{
  "name": "sandbox",
  "description": "Workspace agent: run shell commands (any language), read/write workspace files, manage durable memory, inspect the environment.",
  "tools": [
    "terminal",
    "memory",
    "list_files",
    "read_file",
    "list_workspace",
    "read_workspace_file",
    "write_file",
    "inspect_env",
    "write_log",
    "request_permission",
    "add_goal",
    "set_goal_status",
    "read_skill",
    "create_skill"
  ],
  "note": "Network is OFF by default; the operator can enable it at runtime (TUI: /net on), or you can ask for it with request_permission while the operator is present.",
  "mcp_servers": [
    "*"
  ]
}
